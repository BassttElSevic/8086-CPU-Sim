# CGA 文本显示

`sim_cga.c` 模拟的是 PC 的 CGA 文本和基础图形显示适配器。当前主要是文本模式。

## 外部接口

- 显存：`B8000h-BBFFFh`
- CRTC 端口：`3D4h` / `3D5h`
- 模式端口：`3D8h`
- 颜色选择端口：`3D9h`
- 状态端口：`3DAh`

## 主要功能

- 80x25 文本模式；
- 字符/属性对；
- 光标位置；
- 闪烁相位；
- 文本页显示；
- 宿主窗口观察已提交 VRAM。

## 运行方式

CPU 写 VRAM 之后，真正可见的画面在提交边沿才更新。宿主窗口只读模拟显存，然后显示出来

