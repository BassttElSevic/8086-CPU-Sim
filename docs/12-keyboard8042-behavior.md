# 8042 键盘控制器

键盘模块把宿主输入转成 8042/PC 键盘可见的事件。

## 外部接口

- 典型端口：`60h` 和 `64h`
- 输出：键盘数据字节、状态位、IRQ1

## 主要功能

- 扫描码输入缓冲；
- 状态寄存器；
- 发送中断请求；
- 基础键盘命令处理；
- 退格、回车、方向键、功能键和常见字符输入。

## 宿主交互

宿主窗口只负责采集按键。按键先进入模拟键盘控制器，再由控制器以 IRQ 的形式通知 CPU。


